#include <stdio.h>

int main(){

    float x, y;

    printf("Dg x: ");
        scanf("%f", &x);
    printf("Dg y: ");
        scanf("%f",&y);

    float Area = (x*y);

    printf("valor area: %f",Area);

    return 0;
}